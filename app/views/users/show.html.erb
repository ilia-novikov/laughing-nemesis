<div class="container-fluid">
	<div class="row-fluid">
		<div class="span12">
			<h1>
				<%= gravatar_image_tag @user.email, :class => "img-circle", :alt => @user.name %>
				<%= @user.name %>
			</h1>
		</div>
	</div>
</div>